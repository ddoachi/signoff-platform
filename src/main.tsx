import React from 'react';
import ReactDOM from 'react-dom/client';
import SignoffDashboard from './dashboard/SignoffDashboard';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SignoffDashboard />
  </React.StrictMode>,
);
