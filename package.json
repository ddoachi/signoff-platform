{
  "name": "@signoff/db-codegen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:generate": "tsx scripts/codegen-db/index.ts",
    "db:generate:prod": "tsx scripts/codegen-db/index.ts --env=production",
    "db:generate:dry-run": "tsx scripts/codegen-db/index.ts --dry-run",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "pg": "^8.13.1",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.4"
  }
}
